/* Identify : 
    - Arr / string + subarray/substring + largest/min + k
    - Sometimes k can be ask to find
    - Fix size window : Initially we fix window size + add the next one by removeing first one from the window 
    - Variable size : Will be ask to find window satisfying given condition
*/
